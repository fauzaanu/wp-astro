---
const { category } = Astro.props;
---

<a 
    href={`/categories/${category.slug}/`}
    class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-gray-100 text-gray-800 hover:bg-gray-200 transition-colors"
>
    {category.name}
</a>
